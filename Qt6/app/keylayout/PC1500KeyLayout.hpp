#pragma once
#include "KeyLayoutTypes.hpp"

// PC-1500/PC-1500A body-art key layout. Same layout applies to both models
// (identical art apart from the model-name text).
namespace KeyLayout {
const Layout& pc1500Layout();
} // namespace KeyLayout
