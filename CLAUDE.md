# Calc-U-1600

Before removing, simplifying or "fixing" code in a review or cleanup, read `docs/Decisions.md`. It lists deliberate choices that look wrong at first sight. When a recurring review question gets settled, add it there.
